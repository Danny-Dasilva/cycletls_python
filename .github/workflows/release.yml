name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:  # Allow manual trigger for testing

jobs:
  build-wheels:
    name: Build wheel for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.12']

    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26'

      - name: Install Python build tools
        run: |
          python -m pip install --upgrade pip
          pip install build wheel setuptools

      - name: Install Zig (Linux, for glibc 2.17 target)
        if: runner.os == 'Linux'
        run: |
          ZIG_VERSION=0.13.0
          wget -q "https://ziglang.org/download/${ZIG_VERSION}/zig-linux-x86_64-${ZIG_VERSION}.tar.xz"
          tar xf "zig-linux-x86_64-${ZIG_VERSION}.tar.xz"
          ZIG="$PWD/zig-linux-x86_64-${ZIG_VERSION}/zig"
          mkdir -p "$HOME/.local/bin"
          printf '#!/bin/sh\nexec "%s" cc -target x86_64-linux-gnu.2.17 "$@"\n' "$ZIG" > "$HOME/.local/bin/zcc"
          chmod +x "$HOME/.local/bin/zcc"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Build Go shared library (Linux)
        if: runner.os == 'Linux'
        env:
          CC: zcc
        run: |
          cd golang
          mkdir -p ../cycletls/dist
          ARCH=$(go env GOARCH | sed 's/amd64/x64/')
          go build -buildmode=c-shared -ldflags="-s -w" -o "../cycletls/dist/libcycletls-linux-${ARCH}.so" .
          ls -lh ../cycletls/dist/

      - name: Build Go shared library (macOS)
        if: runner.os == 'macOS'
        run: |
          cd golang
          mkdir -p ../cycletls/dist
          ARCH=$(go env GOARCH | sed 's/amd64/x64/')
          go build -buildmode=c-shared -ldflags="-s -w" -o "../cycletls/dist/libcycletls-darwin-${ARCH}.dylib" .
          ls -lh ../cycletls/dist/

      - name: Build Go shared library (Windows)
        if: runner.os == 'Windows'
        run: |
          cd golang
          New-Item -ItemType Directory -Force -Path ../cycletls/dist
          $ARCH = (go env GOARCH) -replace "amd64", "x64"
          go build -buildmode=c-shared -ldflags="-s -w" -o ("../cycletls/dist/cycletls-win-" + $ARCH + ".dll") .
          Get-ChildItem ../cycletls/dist/
        shell: pwsh

      - name: Build Python wheel
        run: python -m build --wheel

      - name: Build source distribution (Ubuntu only)
        if: runner.os == 'Linux'
        run: python -m build --sdist

      - name: List dist files
        run: ls -R dist/
        shell: bash

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v7
        with:
          name: wheel-${{ matrix.os }}
          path: dist/*.whl

      - name: Upload sdist artifact (Ubuntu only)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v7
        with:
          name: sdist
          path: dist/*.tar.gz

  publish-to-pypi:
    name: Publish to PyPI
    needs: build-wheels
    runs-on: ubuntu-latest
    # Only publish on the upstream repo, not forks
    if: startsWith(github.ref, 'refs/tags/') && github.repository == 'Danny-Dasilva/cycletls_python'
    environment:
      name: pypi
      url: https://pypi.org/p/cycletls
    permissions:
      id-token: write  # For OIDC trusted publishing
      contents: read

    # NOTE: Trusted publishing must be configured on pypi.org:
    # 1. Go to https://pypi.org/manage/project/cycletls/settings/publishing/
    # 2. Add a new "pending publisher" with:
    #    - Owner: <github-username-or-org>
    #    - Repository: cycletls_python
    #    - Workflow: release.yml
    #    - Environment: pypi

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist-artifacts

      - name: Prepare dist directory
        run: |
          mkdir -p dist
          find dist-artifacts -name '*.whl' -exec cp {} dist/ \;
          find dist-artifacts -name '*.tar.gz' -exec cp {} dist/ \;
          ls -lh dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        # No password/token needed - uses OIDC trusted publishing

  create-github-release:
    name: Create GitHub Release
    needs: build-wheels
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v7

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist-artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release-assets
          find dist-artifacts -name '*.whl' -exec cp {} release-assets/ \;
          find dist-artifacts -name '*.tar.gz' -exec cp {} release-assets/ \;
          ls -lh release-assets/

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          files: release-assets/*
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref, 'rc') || contains(github.ref, 'alpha') || contains(github.ref, 'beta') }}

  deploy-pages:
    name: Deploy PEP 503 index to GitHub Pages
    needs: create-github-release
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist-artifacts

      - name: Prepare dist directory
        run: |
          mkdir -p dist
          find dist-artifacts -name '*.whl' -exec cp {} dist/ \;
          find dist-artifacts -name '*.tar.gz' -exec cp {} dist/ \;
          ls -lh dist/

      - name: Generate PEP 503 index
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          BASE_URL="https://github.com/${{ github.repository }}/releases/download/${TAG}"
          python scripts/generate_pep503_index.py \
            --base-url "$BASE_URL" \
            --output-dir site/simple \
            dist/*

      - name: Deploy to GitHub Pages
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          cd site
          git init
          git checkout -b gh-pages
          git add .
          git commit -m "Deploy PEP 503 index for ${GITHUB_REF}"
          git push --force "https://${GITHUB_ACTOR}:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" gh-pages:gh-pages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
